<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ddvapp="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:orderInCategory="100"
        ddvapp:showAsAction="never" />

    <item
        android:icon="@android:drawable/ic_media_previous"
        android:id="@+id/weekPrev"
        android:showAsAction="always"
        android:orderInCategory="1"
        android:title="@string/main_prev_week"
        ddvapp:showAsAction="always"
        tools:ignore="AppCompatResource"/>

    <item
        android:icon="@android:drawable/ic_media_next"
        android:id="@+id/weekNext"
        android:showAsAction="always"
        android:orderInCategory="2"
        android:title="@string/main_next_week"
        ddvapp:showAsAction="always"
        tools:ignore="AppCompatResource"/>

</menu>
